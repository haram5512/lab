# Full clean Baseline run configuration selected by the short throughput benchmark.
dataset_config: dataset_config.yaml
weights: yolo11n.pt
image_size: 640
batch_size: 16
num_workers: 0
pin_memory: false
persistent_workers: false
learning_rate: 0.0001
epochs: 100
validation_every: 5
train_max_images: 0
val_max_images: 0
output: runs/baseline_clean_full_letterbox
